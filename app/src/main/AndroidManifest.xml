<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nlm.mobilehotelappassignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileHotelAppAssignment">
        <activity android:name=".signUp"></activity>
        <activity android:name=".ViewRoomsDetails" />
        <activity android:name=".ViewRooms" />
        <activity android:name=".bookRoomDetails" />
        <activity android:name=".bookRoom" />
        <activity android:name=".customerHome" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>